<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".NewsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsAppMVVMArchitecture"
        tools:targetApi="31">
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">

            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.topheadlinepagination.TopHeadlinePaginationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.offlinearticles.OfflineArticlesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.topheadline.TopHeadlineActivity"
            android:exported="false" />
        <activity
            android:name=".ui.newssources.NewsSourcesActivity"
            android:exported="false" />
        <activity android:name=".ui.countries.CountriesActivity"
            android:exported="true"/>

        <activity android:name=".ui.newslist.NewsListActivity"
            android:exported="true"/>
        <activity android:name=".ui.language.LanguageActivity"
            android:exported="true"/>
        <activity android:name=".ui.search.SearchActivity"
            android:exported="true"/>

    </application>

</manifest>